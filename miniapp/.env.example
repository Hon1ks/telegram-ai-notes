# Production / Pages deploy
VITE_API_URL=https://notes-bot.your-subdomain.workers.dev

# Local browser preview (see .env.development):
# VITE_API_URL=          — empty = proxy /api → localhost:8787
# VITE_DEV_PREVIEW=true
# Run: npm run gen:auth  — writes VITE_DEV_INIT_DATA to .env.development.local
